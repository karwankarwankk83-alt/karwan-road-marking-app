name: Build KS Native AR Meter

on:
  workflow_dispatch:
  push:
    paths:
      - 'android-ar-meter/**'
      - '.github/workflows/android-ar-meter.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - uses: android-actions/setup-android@v3

      - name: Accept Android SDK licenses
        run: yes | sdkmanager --licenses > /dev/null || true

      - name: Install Android SDK 36
        run: sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Build installable APKs
        working-directory: android-ar-meter
        run: gradle :app:assembleDebug :app:assembleRelease --stacktrace --no-daemon

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: KS-Native-AR-Meter-release
          path: android-ar-meter/app/build/outputs/apk/release/*.apk
          if-no-files-found: error
          retention-days: 30

      - name: Upload fallback debug APK
        uses: actions/upload-artifact@v4
        with:
          name: KS-Native-AR-Meter-debug
          path: android-ar-meter/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 30
